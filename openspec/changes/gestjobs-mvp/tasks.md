# Tasks: gestjobs MVP

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 1500–2200 (bootstrap + 10-table schema + 17 design-listed files + tests + README/CI) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 Foundation → PR 2 Platforms → PR 3 Contacts+Resumes → PR 4 Applications → PR 5 Reminders+Dashboard → PR 6 Verification → Deliverable 7 Publication |
| Delivery strategy | ask-always |
| Chain strategy | pending |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

### Suggested Work Units

| Unit | Goal | Likely PR | Notes |
|------|------|-----------|-------|
| 1 | Next.js 15 bootstrap + Supabase schema/RLS/types/seed + auth scaffolding | PR 1 | base: main; standalone preview deploy |
| 2 | Platforms inference + seed directory + searchable combobox + custom persistence | PR 2 | base: PR 1 branch (or main if stacked) |
| 3 | Contacts directory + Resumes versioned upload + metadata | PR 3 | base: PR 1 branch; independent of PR 2 |
| 4 | Applications CRUD + status workflow + history + job proposal (text/file/URL) + attachments | PR 4 | base: PR 2 + PR 3 branches |
| 5 | Reminder scheduling (DB trigger + cron) + Resend email + Dashboard counters/pending list | PR 5 | base: PR 4 branch |
| 6 | E2E verification + README + CI + Vitest harness | PR 6 | base: PR 5 branch |
| 7 | **Publication** — push to a new **public GitHub repository** | Deliverable (no PR) | post-implementation; outside `sdd-apply` |

## Phase 1: Foundation (PR 1)

- [ ] 1.1 Bootstrap Next.js 15 App Router project with TypeScript + Tailwind at repo root. Create `package.json`, `tsconfig.json`, `next.config.mjs`, `tailwind.config.ts`, `postcss.config.mjs`, `src/app/layout.tsx`, `src/app/page.tsx`.
- [ ] 1.2 Add `.env.example` + `.env.local` placeholders for `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `RESEND_API_KEY`, `CRON_SECRET`.
- [ ] 1.3 Create `src/lib/supabase/client.ts` + `src/lib/supabase/server.ts` with cookie-based SSR client; add `@supabase/ssr`, `@supabase/supabase-js` to `package.json`.
- [ ] 1.4 Create `supabase/migrations/001_initial_schema.sql` with tables `statuses`, `platforms`, `applications`, `application_status_history`, `contacts`, `application_contacts`, `resumes`, `application_resumes`, `reminder_dispatches`. Include `user_id` on every tenant table; enable RLS and add per-user policies.
- [ ] 1.5 Create `supabase/migrations/002_storage_buckets.sql` enabling `resumes` and `proposals` buckets with `auth.uid()` RLS policies and 1-hour signed URL convention.
- [ ] 1.6 Create `supabase/seed.sql` with default statuses (`Applied`, `Screening`, `Interview`, `Offer`, `Hired`, `Rejected`, `Withdrawn`; `Hired/Rejected/Withdrawn` terminal) and global seed platforms (LinkedIn, Indeed, Glassdoor, Greenhouse, Lever, Computrabajo, Gallito Uruguay, BuscoJobs, Workable, SmartRecruiters).
- [ ] 1.7 Commit `src/lib/supabase/database.types.ts` stub with `// generated by supabase gen types typescript` header.
- [ ] 1.8 Create `src/app/(auth)/login/page.tsx` — Supabase magic-link form calling `signInWithOtp` server action.
- [ ] 1.9 Create `src/middleware.ts` refreshing Supabase session on every request.
- [ ] 1.10 **Verify**: `pnpm dev` boots; `supabase db reset` applies migrations + seed; magic-link login returns 200; cross-user RLS denied in SQL test.
- [ ] 1.11 **Rollback**: delete Vercel/Supabase projects; remove `src/` and `supabase/`; repo returns to empty skeleton.

## Phase 2: Platforms (PR 2)

- [ ] 2.1 Create `src/lib/platforms/infer.ts` with `normalizeHostname(url)` + `inferPlatformFromUrl(url, platforms)`; pure, deterministic, typed `Platform` export.
- [ ] 2.2 Create `src/lib/platforms/seed.ts` mirroring `supabase/seed.sql` for client-side combobox initialization.
- [ ] 2.3 Create `src/components/platform-combobox.tsx` — accessible combobox (Headless UI or Radix) with search, free-text custom entry, and `onConfirm` callback that upserts a custom platform row.
- [ ] 2.4 Add `src/app/applications/actions.ts::upsertCustomPlatform(name, hostname)` Server Action enforcing `auth.uid()` and uniqueness on `(user_id, hostname)`.
- [ ] 2.5 **Verify**: known hostnames resolve; unknown hostname triggers fallback; custom platform persists and reappears on next session.
- [ ] 2.6 **Rollback**: revert PR 2 only — combobox is unused until PR 4.

## Phase 3: Contacts + Resumes (PR 3)

- [ ] 3.1 Create `src/app/contacts/page.tsx` + `src/app/contacts/actions.ts` — CRUD Server Actions with Zod validation (`name`, `email`, `phone`, `linkedin_url`, `notes`).
- [ ] 3.2 Create `src/lib/validation/contact.ts` rejecting empty `name`.
- [ ] 3.3 Create `src/app/resumes/page.tsx` + `src/app/resumes/actions.ts` — upload (PDF/DOCX, ≤ configured MB), label input, server-side `file_hash`; reject oversize/invalid MIME.
- [ ] 3.4 Create `src/lib/validation/resume.ts` — file size + MIME allow-list; Zod for label.
- [ ] 3.5 **Verify**: upload stored under `resumes/{user_id}/...`; signed URL 200; cross-user RLS denied; oversize file rejected at action boundary.
- [ ] 3.6 **Rollback**: revert PR 3 only — Contacts/Resumes are independent modules.

## Phase 4: Applications + Status Workflow (PR 4)

- [ ] 4.1 Create `src/lib/validation/application.ts` — Zod schemas rejecting empty `company_name`/`position_title`; `platform_url` must parse as HTTP(S); `proposalUrl` validated separately.
- [ ] 4.2 Create `src/app/applications/page.tsx` — list view grouped by status; joins `applications × platforms × statuses`.
- [ ] 4.3 Create `src/app/applications/new/page.tsx` — form integrating `platform-combobox` + URL inference + job-proposal (text/file/URL) inputs.
- [ ] 4.4 Create `src/app/applications/actions.ts` — `createApplication`, `updateApplication`, `deleteApplication` Server Actions; FK cascade + RLS.
- [ ] 4.5 Create `src/app/applications/[id]/page.tsx` — detail view: status timeline, attached resume, linked contacts with roles, job proposal.
- [ ] 4.6 Add `changeApplicationStatus(applicationId, toStatusId)` — transactional insert into `application_status_history` + update `applications.updated_at`.
- [ ] 4.7 Add `attachResume` + `attachContact` — replaces prior `application_resumes` (PK on `application_id`); inserts `application_contacts`.
- [ ] 4.8 **Verify**: create → status change (history row) → attach resume + contact → detach (join deleted, contact retained) → delete (cascade removes history + attachments).
- [ ] 4.9 **Rollback**: revert PR 4 — Applications depend on PR 2 + PR 3, which remain deployable.

## Phase 5: Reminders + Dashboard (PR 5)

- [ ] 5.1 Create `src/lib/reminders/schedule.ts` — pure `computeNextReminderAt(applicationDate, lastStatusChangeAt, statusIsTerminal): Date | null`; `null` for terminal statuses.
- [ ] 5.2 Add `supabase/migrations/003_reminder_trigger.sql` — on `application_status_history` insert, recompute `applications.next_reminder_at` via SQL function `compute_next_reminder_at()`.
- [ ] 5.3 Create `src/lib/email/resend.ts` — `sendReminderEmail(applicationId, user)` using Resend SDK; writes success/failure to `reminder_dispatches`; idempotent on `(application_id, sent_at::date)`.
- [ ] 5.4 Create `src/app/api/cron/reminders/route.ts` — POST only, guarded by `CRON_SECRET`; selects due reminders; dispatches email; updates `reminder_dispatches`.
- [ ] 5.5 Add `vercel.json` with cron `"0 9 * * *"` → `/api/cron/reminders`.
- [ ] 5.6 Create `src/app/dashboard/page.tsx` — RSC reading status counters + pending reminders sorted `next_reminder_at asc`, dismissed excluded; quick-link cards to application detail.
- [ ] 5.7 **Verify**: seed overdue application → cron POST 200 + dispatch row written; dashboard lists pending; terminal-status applications excluded.
- [ ] 5.8 **Rollback**: revert PR 5 — Dashboard degrades to empty pending section; cron route returns 410.

## Phase 6: Verification + README (PR 6)

- [ ] 6.1 Write `README.md` — quickstart, env setup, Supabase migrate/seed commands, Resend config, deploy steps.
- [ ] 6.2 Write `.github/workflows/ci.yml` — `pnpm install` + `pnpm typecheck` + `pnpm test`.
- [ ] 6.3 Add minimal Vitest config + unit tests for `inferPlatformFromUrl`, `computeNextReminderAt`, Zod schemas; record command in `openspec/config.yaml#rules.apply.test_command`.
- [ ] 6.4 Smoke checklist mapped to spec scenarios (applications, platforms, reminders, dashboard, contacts, resumes); results captured in `verify-report.md`.
- [ ] 6.5 **Verify**: every spec acceptance criterion passes; CI green on PR 6.
- [ ] 6.6 **Rollback**: revert PR 6 — README/CI revert does not affect deployed app behavior.

## Phase 7: Publication Readiness (post-implementation deliverable, no PR)

- [ ] 7.1 Confirm with user: GitHub org/account + desired repo name + visibility (**public**).
- [ ] 7.2 Add `LICENSE` (MIT recommended) and `CODE_OF_CONDUCT.md` if missing — required for a public open-source repo.
- [ ] 7.3 Sanity-check `.gitignore` (`node_modules`, `.env.local`, `.next`, `coverage`, Supabase local artifacts).
- [ ] 7.4 Confirm no secrets/PII in tracked files (`git grep -nE '(sk_live|service_role|RESEND_API_KEY)'` must return no matches).
- [ ] 7.5 Initialize remote, push `main` (or feature-branch-chain tracker branch) per chosen chain strategy.
- [ ] 7.6 Configure repo settings: branch protection on `main`, Issues enabled, README renders, Vercel deploy link in repo description.
- [ ] 7.7 Tag first public release `v0.1.0-mvp` and draft GitHub Release notes summarizing capabilities.
- [ ] 7.8 **Rollback**: unpublish or transfer repo; no app data affected (DB lives in Supabase).